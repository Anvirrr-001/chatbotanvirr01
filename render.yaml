services:
  - type: web
    name: chatbot-anvirr
    env: node
    plan: free
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: ADMIN_PASSWORD
        sync: false
      - key: NODE_VERSION
        value: 18
      - key: PORT
        value: 10000
